name: Publish

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  tauri:
    uses: ./.github/workflows/release-tauri.yml
    secrets: inherit
  egui:
    uses: ./.github/workflows/release-egui.yml
    secrets: inherit
