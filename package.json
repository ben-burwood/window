{
  "name": "viewers",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for four desktop file-viewer apps (data-framer, map-windower, doc-viewer, image-shutter).",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "echo \"per-app build wiring added in Phase 2\" && exit 0"
  }
}
